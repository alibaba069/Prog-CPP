#include <iostream>
#include <cctype>

/* Character Funktionen 
// Testing 

char wort 

isalpha(wort) => True if wort is a letter
isalnum(wort) => True if wort is a letter or a digit
isdigit(wort) => True if wort is a digit
islower(wort) => True if wort is a lowercase letter
isprint(wort) => True if wort is a printable character
ispunct(wort) => True if wort is a punctuable character
isupper(wort) => True if wort is a uppercase letter
isspace(wort) => True if wort is a whitespace

//converting
tolower(wort) => returns lowercase of wort
toupper(wort) => returns uppercase of wort
*/
 
